{% extends 'admin/base.html.twig' %}

{% block title %}{{ 'trans.Create new package' | trans }}{% endblock %}

{% block body %}
<div class="container mt-3">
    <div class="row mb-2">
        <div class="col-md-6">
            <h1>{{ 'trans.Create new package' | trans }}</h1>
        </div>
        <div class="col-md-6 text-md-right">
            <a href="{{ path('app_admin_package_list') }}">{{ 'trans.back to list' | trans }}</a>
        </div>
    </div>

    {{ include('admin/package/_form.html.twig') }}
</div>
{% endblock %}
