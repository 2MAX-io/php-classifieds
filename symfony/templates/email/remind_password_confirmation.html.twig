{# @var user \App\Entity\User #}

<p>
    {{ 'trans.If you did not requested password reset, ignore this email, and do NOT click link bellow' | trans }}.
</p>

<p>
    {{ 'trans.If you have requested password reset, please click link bellow to confirm' | trans }}.
</p>

<p>
    {{ 'trans.When you confirm password reset, your password would be set to' | trans }}:
</p>

<pre>{{- user.plainPassword -}}</pre>

<p>{{ 'trans.for account' | trans }}</p>

<pre>{{- user.username -}}</pre>

<div>
    <a href="{{ url('app_remind_password_confirm', {'token': token}) }}">{{ 'trans.I confirm password reset' | trans }}</a>

    <p>
        {{ 'trans.If the link above does not work, try to copy the link below to your browser' | trans }}:
    </p>

    <pre>{{ url('app_remind_password_confirm', {'token': token}) }}</pre>
</div>
