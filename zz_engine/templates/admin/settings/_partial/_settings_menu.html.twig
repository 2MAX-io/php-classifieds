<div class="mb-2">
    <a href="{{ path('app_admin_settings') }}" class="btn btn-secondary">{{ 'trans.Settings' | trans }}</a>
    <a href="{{ path('app_admin_settings_payment_invoice') }}" class="btn btn-secondary">{{ 'trans.Payment / Invoice Settings' | trans }}</a>
    <a href="{{ path('app_admin_settings_login') }}" class="btn btn-secondary">{{ 'trans.Login Settings' | trans }}</a>

    <a href="{{ path('app_admin_logo_upload') }}" class="btn btn-secondary">{{ 'trans.Webpage Logo' | trans }}</a>
</div>
