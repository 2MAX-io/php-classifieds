{% extends 'admin/base.html.twig' %}

{% block title %}{{ 'trans.Edit featured package' | trans }}{% endblock %}

{% block body %}
    <div class="container mt-3">
        <div class="row mb-2">
            <div class="col-md-6">
                <h1>{{ 'trans.Edit featured package' | trans }}</h1>
            </div>
            <div class="col-md-6 text-md-right">
                <a href="{{ path('app_admin_featured_package_index') }}">{{ 'trans.back to list' | trans }}</a>
                {{ include('admin/featured_package/_delete_form.html.twig') }}
            </div>
        </div>

        {{ include('admin/featured_package/_form.html.twig', {'button_label': 'trans.Update' | trans}) }}
    </div>

{% endblock %}
