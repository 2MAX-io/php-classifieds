{% extends 'admin/base.html.twig' %}

{% block title %}{{ 'trans.Add custom field' | trans }}{% endblock %}

{% block body %}
<div class="container mt-3">
    <div class="row mb-2">
        <div class="col-md-6">
            <h1>{{ 'trans.Add custom field' | trans }}</h1>
        </div>
        <div class="col-md-6 text-md-right">
            <a href="{{ path('app_admin_custom_field_index') }}">{{ 'trans.back to list' | trans }}</a>
        </div>
    </div>

    {{ include('admin/custom_field/_form.html.twig') }}
</div>
{% endblock %}
