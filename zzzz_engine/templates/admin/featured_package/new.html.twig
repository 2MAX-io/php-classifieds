{% extends 'admin/base.html.twig' %}

{% block title %}{{ 'trans.Create new featured package' | trans }}{% endblock %}

{% block body %}
<div class="container mt-3">
    <div class="row mb-2">
        <div class="col-md-6">
            <h1>{{ 'trans.Create new featured package' | trans }}</h1>
        </div>
        <div class="col-md-6 text-md-right">
            <a href="{{ path('app_admin_featured_package_index') }}">{{ 'trans.back to list' | trans }}</a>
        </div>
    </div>

    {{ include('admin/featured_package/_form.html.twig') }}
</div>
{% endblock %}
